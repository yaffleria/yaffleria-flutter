# Android에서 앱 테스트하기 - 해결 방법

## 🚨 현재 문제

Flutter 3.38.5 버전에서 **ShaderCompilerException** 오류가 발생하여 Android와 Windows 빌드가 실패합니다:

```
ShaderCompilerException: Shader compilation of
"C:\flutter\packages\flutter\lib\src\material\shaders\stretch_effect.frag"
failed with exit code -1073741819
```

## ✅ 해결 방법 (선택)

### 방법 1: 웹 브라우저에서 계속 사용 (가장 빠름) ⭐

현재 웹에서는 정상 작동합니다! 학습용으로는 충분합니다:

```bash
flutter run -d chrome
```

**장점:**

- ✅ 즉시 사용 가능
- ✅ Hot Reload 지원
- ✅ 모든 예제 정상 작동

### 방법 2: Flutter SDK 재설치 (권장)

안정적인 Flutter 버전을 사용하려면 Flutter SDK를 재설치하는 것이 좋습니다.

#### 단계 1: 현재 Flutter 제거

1. 환경 변수에서 Flutter 경로 제거:

   - "시스템 환경 변수 편집" 검색
   - Path에서 `C:\flutter\bin` 제거

2. `C:\flutter` 폴더 삭제 (또는 백업)

#### 단계 2: 안정적인 Flutter 설치

```bash
# 1. C 드라이브에 새 위치로 이동
cd C:\

# 2. Flutter stable 브랜치 클론
git clone https://github.com/flutter/flutter.git -b stable --depth 1

# 3. Flutter 디렉토리로 이동
cd flutter

# 4. Flutter doctor 실행
bin\flutter doctor
```

#### 단계 3: 환경 변수 설정

1. "시스템 환경 변수 편집" 열기
2. Path에 `C:\flutter\bin` 추가

#### 단계 4: 확인

```bash
# 새 터미널 열고 확인
flutter --version

# 프로젝트로 돌아가서 테스트
cd "C:\Users\nenya\OneDrive\바탕 화면\git\flutter-sample"
flutter run -d emulator-5554
```

### 방법 3: 특정 Flutter 버전으로 전환

현재 Flutter 설치를 유지하면서 버전만 변경:

```bash
# Flutter SDK 디렉토리로 이동
cd C:\flutter

# 사용 가능한 태그 확인
git fetch --tags

# 안정적인 버전으로 전환 (예: 3.24.0)
git checkout 3.24.0

# Flutter 업데이트
flutter doctor
```

**추천 버전:**

- `3.24.0` - 안정적
- `3.22.0` - 매우 안정적
- `3.19.0` - 장기 지원

### 방법 4: Android 빌드 임시 우회 (실험적)

Material 3 대신 Material 2 사용으로 일부 shader 문제를 우회할 수 있습니다 (이미 main.dart에서 `useMaterial3: false`로 설정됨).

추가로 시도할 수 있는 것:

**android/app/build.gradle 수정:**

```gradle
android {
    // ... 기존 설정 ...

    buildTypes {
        debug {
            // Shader 최적화 비활성화
            minifyEnabled false
            shrinkResources false
        }
    }
}
```

## 📊 현재 작동 상태

| 플랫폼             | 상태    | 비고                    |
| ------------------ | ------- | ----------------------- |
| Chrome (웹)        | ✅ 작동 | 추천: 학습용으로 완벽   |
| Edge (웹)          | ✅ 작동 | -                       |
| Android 에뮬레이터 | ❌ 실패 | ShaderCompilerException |
| Windows 데스크톱   | ❌ 실패 | 빌드 파일 생성 실패     |

## 🎯 추천 방안

**학습 목적이라면:**

1. **지금 당장**: Chrome 웹 브라우저에서 계속 사용 (`flutter run -d chrome`)
   - 모든 기능 정상 작동
   - Hot Reload 지원
   - 학습에는 충분

**본격적인 개발을 위해:** 2. **시간 있을 때**: Flutter SDK 재설치 (방법 2)

- 가장 안정적
- 모든 플랫폼 지원

## 💡 참고

- 이 문제는 Flutter 3.38.5의 알려진 버그입니다
- 웹 빌드는 정상 작동하므로 학습에는 문제없습니다
- 향후 Flutter 업데이트에서 수정될 예정입니다

---

**작성일**: 2025-12-18  
**Flutter 버전**: 3.38.5 (문제 버전)
